/* 🌟 RESET STYLES (Keeps things clean) */
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
}

/* 🎨 BODY BACKGROUND & TEXT */
body {
    background-color: #fdf6e3; /* Cozy beige */
    color: #333;
    padding: 20px;
}

/* 🌟 HEADER */
.landing {
    background-color: #ff9f20;
    color: white;
    padding: 40px;
    text-align: center;
    font-size: 24px;
}

/* 🏠 3-COLUMN LAYOUT */
.container {
    display: flex;
    max-width: 1100px;
    margin: 20px auto;
    justify-content: space-between;
    gap: 10px;
}

/* 📦 COLUMN SETTINGS */
.left-column, .middle-column, .right-column {
    padding: 20px;
    min-height: 200px; /* Keeps things even */
}

/* 🟦 LEFT & RIGHT COLUMN (SIDEBARS) */
.left-column, .right-column {
    width: 25%;
    background-color: #dafdff; /* Light blue */
    border: 1px dashed #ff9f20;
}

/* 📜 MIDDLE COLUMN (MAIN CONTENT) */
.middle-column {
    width: 50%;
    background-color: white;
    border: 1px solid #ff9f20;
}

/* 🏁 FOOTER */
.footer {
    background-color: rgba(0, 0, 0, 0.5);
    color: white;
    padding: 10px;
    margin-top: 20px;
    text-align: center;
}

/* 🎵 MUSIC PLAYER */
#music-container {
    position: fixed;
    bottom: 20px;
    left: 20px;
    background: rgba(0, 0, 0, 0.7);
    padding: 10px;
    border-radius: 5px;
    display: flex;
    align-items: center;
    gap: 5px;
}

/* 🎧 PLAYER CONTROLS */
#controls button {
    background: none;
    border: none;
    color: white;
    font-size: 18px;
    cursor: pointer;
}

#now-playing {
    color: white;
    margin-left: 10px;
}
